"use strict";
// A generic interface works like a contract for objects but allows flexibility with types.
const stringBox = { value: "Hello" }; // T is 'string'
const numberBox = { value: 42 }; // T is 'number'
