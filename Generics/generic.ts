//Generics are like placeholders for types. 
// Instead of writing a specific type (e.g., number or string), you use a generic (like T) and 
// decide the type later when you use it. This makes your code reusable and type-safe.

