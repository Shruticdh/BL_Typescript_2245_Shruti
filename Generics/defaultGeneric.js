"use strict";
// Sometimes, you want to provide a default type so the user doesn’t have to specify it.
const defaultStorage = { value: "Default" }; // T defaults to 'string'
const numberStorage = { value: 42 }; // T is 'number'
